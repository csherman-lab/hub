# 3D character models

Drop `.glb` files here. They load automatically in voice/video calls.

Example: `jules.glb` → used when Jules is your agent.

See [docs/CHARACTER_ASSETS.md](../docs/CHARACTER_ASSETS.md) for how to add your own character from a zip on your Mac.
